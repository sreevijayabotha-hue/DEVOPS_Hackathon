apiVersion: v1
kind: Service
metadata:
  name: {{ include "spring-employee-app.fullname" . }}
  labels:
    app: {{ include "spring-employee-app.name" . }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.port }}
  selector:
    app: {{ include "spring-employee-app.name" . }}

